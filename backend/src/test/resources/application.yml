spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

dukaanai:
  jwt:
    secret: "test-secret-key-minimum-256-bits-long-for-spring-security-unit-tests!"
    expiration-ms: 3600000
  ai:
    provider: mock
  payment:
    webhook-secret: dukaansaathi-upi-webhook-test-secret
    verification-provider: manual
